# 🔧 Исправление: Новая Игра Теперь Обнуляет Очки

## 🐛 Проблема

**Симптом:** При нажатии "Новая игра" (PlayButton) очки не обнулялись, игра продолжалась со старыми очками.

**Причина:** `ScoreManager` загружал данные из **старого слота 0** в методе `Start()`, перезаписывая данные, которые `InGameSaveManager` уже установил из **нового слота**.

## ✅ Решение

### Изменения в ScoreManager.cs:

#### 1. Убрана автозагрузка в Start()

**Было:**
```csharp
void Start()
{
    LoadProgress(); // ← Загружал из слота 0!
    UpdateScoreUI();
}
```

**Стало:**
```csharp
void Start()
{
    // НЕ загружаем прогресс здесь!
    // InGameSaveManager уже загрузил данные в Awake()
    UpdateScoreUI();
}
```

#### 2. Исправлено сохранение в правильный слот

**Было:**
```csharp
private void SaveProgress()
{
    SaveData saveData = SaveSystem.Load(); // ← Всегда слот 0!
    saveData.currentScore = currentScore;
    SaveSystem.Save(saveData); // ← Всегда слот 0!
}
```

**Стало:**
```csharp
private void SaveProgress()
{
    if (InGameSaveManager.Instance != null)
    {
        SaveData saveData = InGameSaveManager.Instance.GetCurrentSaveData();
        saveData.currentScore = currentScore;
        saveData.highScore = highScore;
        
        int currentSlot = InGameSaveManager.Instance.GetCurrentSlotIndex();
        SaveSystem.SaveToSlot(saveData, currentSlot); // ← Правильный слот!
    }
}
```

#### 3. Удален метод LoadProgress()
Больше не нужен, так как `InGameSaveManager` управляет загрузкой.

## 🔄 Как Теперь Работает

### Последовательность загрузки:

```
1. MainMenu.OnPlayButton()
   → Создает НОВОЕ сохранение в слоте 1
   → PlayerPrefs.SetInt("CurrentSaveSlot", 1)
   → Загружает GameScene

2. InGameSaveManager.Awake() [выполняется ПЕРВЫМ]
   → currentSlotIndex = PlayerPrefs.GetInt("CurrentSaveSlot") // = 1
   → currentSaveData = SaveSystem.LoadFromSlot(1) // Новое сохранение!
   → ApplySaveData()
      → ScoreManager.Instance.SetScore(0) // ← Устанавливает 0!
      → ScoreManager.Instance.SetHighScore(0)

3. ScoreManager.Awake()
   → Создает Instance

4. ScoreManager.Start() [выполняется ПОСЛЕ InGameSaveManager]
   → UpdateScoreUI() // Просто обновляет UI
   → НЕ загружает данные! ← ИСПРАВЛЕНИЕ!
```

### Результат:
- ✅ Очки = 0
- ✅ Лучший счет = 0
- ✅ Уровень = 1
- ✅ Новая игра!

## 📊 Сравнение: До и После

### До Исправления:

```
PlayButton нажата
→ Создано новое сохранение в слоте 1 (очки = 0)
→ InGameSaveManager загружает слот 1 (очки = 0)
→ ScoreManager.SetScore(0) ✅
→ ScoreManager.Start() вызывает LoadProgress()
→ LoadProgress() загружает слот 0 (очки = 1250) ❌
→ currentScore = 1250 ❌
→ Игра продолжается со старыми очками!
```

### После Исправления:

```
PlayButton нажата
→ Создано новое сохранение в слоте 1 (очки = 0)
→ InGameSaveManager загружает слот 1 (очки = 0)
→ ScoreManager.SetScore(0) ✅
→ ScoreManager.Start() НЕ загружает данные ✅
→ currentScore = 0 ✅
→ Новая игра с нуля!
```

## 🎯 Проверка Работы

### Тест 1: Новая игра
```
1. Запустите игру
2. Наберите очки (например, 500)
3. Сохранитесь (F5)
4. Выйдите в меню
5. Нажмите "Новая игра" (PlayButton)
6. Проверьте: Очки должны быть 0 ✅
```

### Тест 2: Продолжить
```
1. Запустите игру
2. Наберите очки (например, 1000)
3. Сохранитесь (F5)
4. Выйдите в меню
5. Нажмите "Продолжить" (SavesButton)
6. Проверьте: Очки должны быть 1000 ✅
```

### Тест 3: Несколько сохранений
```
1. Создайте сохранение в слоте 0 (очки = 500)
2. Создайте сохранение в слоте 1 (очки = 1000)
3. Нажмите "Новая игра"
4. Проверьте: Очки = 0, создан слот 2 ✅
5. Нажмите "Продолжить"
6. Проверьте: Загружен слот 2 (последний) ✅
```

## 🔍 Порядок Выполнения (Unity Lifecycle)

### Важно понимать:

```
Awake() вызывается РАНЬШЕ Start()

Порядок:
1. InGameSaveManager.Awake()  ← Загружает данные
2. ScoreManager.Awake()       ← Создает Instance
3. InGameSaveManager.Start()
4. ScoreManager.Start()       ← Обновляет UI (БЕЗ загрузки!)
```

Поэтому `InGameSaveManager` должен загружать данные в `Awake()`, а `ScoreManager` НЕ должен загружать в `Start()`.

## 🎮 Теперь Правильно Работают:

### PlayButton (Новая игра):
- ✅ Создает новое сохранение
- ✅ Обнуляет очки (0)
- ✅ Обнуляет уровень (1)
- ✅ Обнуляет прогресс

### SavesButton (Продолжить):
- ✅ Загружает последнее сохранение
- ✅ Восстанавливает очки
- ✅ Восстанавливает уровень
- ✅ Восстанавливает прогресс

### F5 в игре:
- ✅ Сохраняет в текущий слот
- ✅ Сохраняет правильные очки
- ✅ Обновляет дату/время

## 🐛 Если Проблема Осталась

### Проверьте:

1. **Порядок выполнения скриптов:**
   - Edit → Project Settings → Script Execution Order
   - InGameSaveManager должен быть РАНЬШЕ ScoreManager

2. **Консоль Unity:**
   - Проверьте логи при запуске игры
   - Должно быть: "Загружено сохранение: Новое сохранение"
   - Должно быть: "Создано новое сохранение в слоте X"

3. **Файлы сохранений:**
   - Откройте: `%AppData%\..\LocalLow\[YourCompany]\[YourGame]\Saves\`
   - Проверьте содержимое `save_slot_X.json`
   - currentScore должен быть 0 для нового сохранения

4. **ScoreManager.SetScore():**
   - Добавьте Debug.Log в метод SetScore:
   ```csharp
   public void SetScore(int score)
   {
       Debug.Log($"SetScore вызван: {score}");
       currentScore = score;
       UpdateScoreUI();
   }
   ```

## ✅ Чек-лист Исправления

- [x] Удален LoadProgress() из ScoreManager.Start()
- [x] Исправлен SaveProgress() для сохранения в правильный слот
- [x] Удален метод LoadProgress()
- [x] InGameSaveManager загружает данные в Awake()
- [x] ScoreManager применяет данные через SetScore()
- [ ] Протестирована "Новая игра" (очки = 0)
- [ ] Протестирован "Продолжить" (очки сохранены)
- [ ] Протестировано F5 (сохраняет в правильный слот)

## 🎉 Итог

Проблема была в **конфликте загрузки данных**:
- `InGameSaveManager` загружал из правильного слота
- `ScoreManager` перезаписывал данными из слота 0

Теперь:
- ✅ Только `InGameSaveManager` загружает данные
- ✅ `ScoreManager` получает данные через `SetScore()`
- ✅ Новая игра обнуляет очки
- ✅ Продолжить восстанавливает очки

Все работает правильно! 🚀
