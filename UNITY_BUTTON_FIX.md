# Исправление: Метод не появляется в выпадающем меню

## ❌ Проблема

В выпадающем меню Unity Button → On Click () видно только:
- `MonoScript` вместо `CheckOrderButton()`

## ✅ Решения

### Решение 1: Перекомпилировать скрипты (РЕКОМЕНДУЕТСЯ)

```
1. В Unity нажать: Assets → Reimport All
   ИЛИ
2. Нажать Ctrl+R (перекомпиляция)
   ИЛИ
3. Закрыть Unity и открыть снова
   ИЛИ
4. Изменить любой скрипт (добавить пробел и сохранить)
```

### Решение 2: Проверить что метод public

```csharp
// В UnderCakePlate.cs должно быть:
public void CheckOrderButton()  // ← public обязательно!
{
    // код
}
```

### Решение 3: Проверить что нет ошибок компиляции

```
1. Открыть Console (Window → General → Console)
2. Проверить что нет красных ошибок
3. Если есть ошибки - исправить их
4. Unity перекомпилирует автоматически
```

### Решение 4: Очистить кэш Unity

```
1. Закрыть Unity
2. Удалить папки:
   - Library/
   - Temp/
3. Открыть Unity снова
4. Подождать перекомпиляции
```

---

## 🔧 Правильная настройка кнопки

### Шаг 1: Убедиться что скрипт прикреплен

```
1. Выбрать объект UnderCake в иерархии
2. В Inspector проверить:
   - Есть компонент "Under Cake Plate (Script)"
   - Скрипт не пустой (не "None")
```

### Шаг 2: Настроить кнопку

```
1. Выбрать кнопку "CheckOrderButton"
2. В Inspector найти компонент "Button"
3. В разделе "On Click ()" нажать "+"
4. Перетащить объект UnderCake в поле
5. В выпадающем меню должно быть:
   
   UnderCakePlate
     ├─ enabled
     ├─ checkRadius
     ├─ stackTolerance
     ├─ showDebugInfo
     └─ CheckOrderButton()  ← ВОТ ЭТО!
```

### Шаг 3: Если метода все еще нет

```
Попробуйте:

1. Сохранить скрипт (Ctrl+S)
2. Вернуться в Unity
3. Подождать 2-3 секунды (компиляция)
4. Попробовать снова настроить кнопку
```

---

## 🐛 Частые проблемы

### Проблема 1: "Метод не появляется"

**Причина:** Unity не перекомпилировал скрипт

**Решение:**
```
1. Assets → Reimport All
2. Подождать завершения
3. Попробовать снова
```

### Проблема 2: "Видно только MonoScript"

**Причина:** Скрипт не прикреплен к объекту или есть ошибки

**Решение:**
```
1. Проверить Console на ошибки
2. Убедиться что UnderCakePlate прикреплен к UnderCake
3. Перекомпилировать (Ctrl+R)
```

### Проблема 3: "Кнопка не работает"

**Причина:** Метод не привязан или объект неправильный

**Решение:**
```
1. Убедиться что в On Click () перетащен UnderCake
2. Убедиться что выбран метод CheckOrderButton()
3. Проверить что Runtime Only (не Editor And Runtime)
```

---

## ✅ Проверка правильной настройки

### В Inspector кнопки должно быть:

```
Button (Script)
  Interactable: ✓
  Transition: Color Tint
  
  On Click ()
    List is Empty
    ┌─────────────────────────────────┐
    │ Runtime Only        ▼           │
    │ UnderCake                       │
    │ UnderCakePlate.CheckOrderButton │
    │ ()                              │
    └─────────────────────────────────┘
```

### Тест:

```
1. Запустить игру (Play)
2. Нажать кнопку
3. В Console должно появиться:
   "Проверка заказа: коржей в стопке = X"
```

---

## 📝 Альтернативный способ (если ничего не помогло)

### Использовать код вместо Inspector:

```csharp
// Создать новый скрипт: CheckOrderButtonHandler.cs
using UnityEngine;
using UnityEngine.UI;

public class CheckOrderButtonHandler : MonoBehaviour
{
    public UnderCakePlate underCakePlate; // Перетащить UnderCake
    
    void Start()
    {
        Button button = GetComponent<Button>();
        button.onClick.AddListener(OnButtonClick);
    }
    
    void OnButtonClick()
    {
        if (underCakePlate != null)
        {
            underCakePlate.CheckOrderButton();
        }
    }
}
```

**Настройка:**
```
1. Прикрепить CheckOrderButtonHandler к кнопке
2. Перетащить UnderCake в поле Under Cake Plate
3. Готово!
```

---

## 🎯 Итоговый чек-лист

- [ ] Метод `CheckOrderButton()` имеет модификатор `public`
- [ ] В Console нет ошибок компиляции
- [ ] Unity перекомпилирован (Assets → Reimport All)
- [ ] UnderCakePlate прикреплен к объекту UnderCake
- [ ] Кнопка имеет компонент Button
- [ ] В On Click () перетащен объект UnderCake
- [ ] В выпадающем меню виден метод CheckOrderButton()
- [ ] При нажатии кнопки в игре появляются логи

---

**Попробуйте Assets → Reimport All - это решает проблему в 90% случаев!** 🔄
