# Настройка спавнера тараканов

## Описание
Система автоматически спавнит тараканов в случайные моменты времени в заданной области.

---

## Настройка

### Шаг 1: Создайте префаб таракана (если еще не создан)

1. Перетащите объект таракана из сцены в папку **Assets** (или создайте папку **Prefabs**)
2. Это создаст префаб таракана
3. Убедитесь, что у таракана есть:
   - **Sprite Renderer** (для отображения)
   - **Collider2D** (для взаимодействия)
   - Любые другие компоненты (скрипты движения, анимация и т.д.)

### Шаг 2: Создайте объект спавнера

1. В Hierarchy создайте пустой объект:
   - **ПКМ → Create Empty**
   - Назовите **"EnemySpawner"**

2. Добавьте скрипт:
   - Выберите объект **EnemySpawner**
   - **Add Component → Enemy Spawner**

### Шаг 3: Настройте параметры в Inspector

#### Настройки спавна:
- **Enemy Prefab**: перетащите префаб таракана из Assets
- **Min Spawn Interval**: минимальное время до следующего спавна (например, 5 секунд)
- **Max Spawn Interval**: максимальное время до следующего спавна (например, 15 секунд)

#### Область спавна:
Настройте область, где будут появляться тараканы:
- **Spawn Min X**: минимальная координата X (например, -8)
- **Spawn Max X**: максимальная координата X (например, 8)
- **Spawn Min Y**: минимальная координата Y (например, -4)
- **Spawn Max Y**: максимальная координата Y (например, 4)

💡 **Совет**: Выберите объект EnemySpawner в Hierarchy - в Scene View появится желтая рамка, показывающая область спавна. Красные сферы показывают углы области.

#### Ограничения:
- **Max Enemies**: максимальное количество тараканов одновременно (например, 5)
- **Auto Start**: ☑ (включено) - автоматически начать спавн при старте игры

---

## Использование

### Автоматический запуск
Если **Auto Start** включен, спавн начнется автоматически при запуске игры.

### Управление через скрипт
Вы можете управлять спавнером из других скриптов:

```csharp
// Получить ссылку на спавнер
EnemySpawner spawner = FindObjectOfType<EnemySpawner>();

// Запустить спавн
spawner.StartSpawning();

// Остановить спавн
spawner.StopSpawning();
```

---

## Как это работает

1. **Случайный интервал**: Спавнер ждет случайное время между `minSpawnInterval` и `maxSpawnInterval`
2. **Проверка лимита**: Проверяет, не превышен ли лимит `maxEnemies`
3. **Случайная позиция**: Выбирает случайную позицию в заданной области
4. **Создание таракана**: Спавнит таракана на этой позиции
5. **Отслеживание**: Когда таракан уничтожается (удаляется из сцены), счетчик уменьшается

---

## Примеры настроек

### Редкий спавн (сложность: легкая)
- Min Spawn Interval: **10**
- Max Spawn Interval: **20**
- Max Enemies: **3**

### Средний спавн (сложность: средняя)
- Min Spawn Interval: **5**
- Max Spawn Interval: **15**
- Max Enemies: **5**

### Частый спавн (сложность: сложная)
- Min Spawn Interval: **3**
- Max Spawn Interval: **8**
- Max Enemies: **8**

---

## Настройка области спавна по размеру карты

Если вы используете **InvisibleWalls** для границ карты:

1. Откройте объект **MapBounds** в Inspector
2. Посмотрите значения **Map Width** и **Map Height**
3. Настройте область спавна чуть меньше:
   - Если Map Width = 20, то:
     - Spawn Min X = **-9**
     - Spawn Max X = **9**
   - Если Map Height = 15, то:
     - Spawn Min Y = **-6.5**
     - Spawn Max Y = **6.5**

---

## Отладка

### В консоли Unity будут сообщения:
- `"Спавн тараканов запущен"` - спавн начался
- `"Таракан заспавнен на позиции..."` - создан новый таракан
- `"Таракан уничтожен. Осталось тараканов: X"` - таракан удален
- `"Достигнут лимит тараканов: X"` - достигнут максимум

### Проверка в Scene View:
- Выберите **EnemySpawner** в Hierarchy
- В Scene View появится **желтая рамка** - область спавна
- **Красные сферы** в углах показывают границы

---

## Дополнительные возможности

### Спавн в определенных точках
Если нужно спавнить тараканов только в определенных местах (например, у краев карты):

1. Создайте несколько объектов **EnemySpawner**
2. Настройте разные области для каждого:
   - **LeftSpawner**: только слева
   - **RightSpawner**: только справа
   - **TopSpawner**: только сверху

### Увеличение сложности со временем
Добавьте в другой скрипт:

```csharp
void Start()
{
    StartCoroutine(IncreaseDifficulty());
}

IEnumerator IncreaseDifficulty()
{
    EnemySpawner spawner = FindObjectOfType<EnemySpawner>();
    
    while (true)
    {
        yield return new WaitForSeconds(60f); // Каждую минуту
        
        // Уменьшаем интервал спавна
        spawner.minSpawnInterval = Mathf.Max(2f, spawner.minSpawnInterval - 0.5f);
        spawner.maxSpawnInterval = Mathf.Max(5f, spawner.maxSpawnInterval - 1f);
        
        // Увеличиваем лимит
        spawner.maxEnemies++;
        
        Debug.Log("Сложность увеличена!");
    }
}
```
