# 🎮 Разница Между Кнопками Меню

## 📋 Две Кнопки в MenuPanel

### 1. **PlayButton** - "Новая игра"
- ✅ Создает **новое** сохранение
- ✅ Обнуляет все очки и прогресс
- ✅ Начинает игру с начала
- ✅ Использует первый свободный слот (или перезаписывает слот 0)

### 2. **SavesButton** - "Продолжить"
- ✅ Загружает **последнее** сохранение
- ✅ Сохраняет все очки и прогресс
- ✅ Продолжает игру с сохраненного места
- ✅ Если нет сохранений - создает новое

## 🔧 Как Работают

### PlayButton (Новая игра):

```csharp
public void OnPlayButton()
{
    // 1. Создаем НОВОЕ сохранение
    int newSlotIndex = SaveSystem.CreateNewSave("Новое сохранение");
    
    // 2. Сохраняем индекс слота
    PlayerPrefs.SetInt("CurrentSaveSlot", newSlotIndex);
    
    // 3. Загружаем игру
    SceneManager.LoadScene("GameScene");
    
    // 4. InGameSaveManager загружает НОВОЕ сохранение
    //    → currentScore = 0
    //    → highScore = 0
    //    → currentLevel = 1
}
```

### SavesButton (Продолжить):

```csharp
public void OnSavesButton()
{
    // 1. Находим ПОСЛЕДНЕЕ сохранение
    SaveData lastSave = SaveSystem.GetLastSave(out slotIndex);
    
    // 2. Сохраняем индекс этого слота
    PlayerPrefs.SetInt("CurrentSaveSlot", slotIndex);
    
    // 3. Загружаем игру
    SceneManager.LoadScene("GameScene");
    
    // 4. InGameSaveManager загружает СУЩЕСТВУЮЩЕЕ сохранение
    //    → currentScore = 1250 (из сохранения)
    //    → highScore = 2000 (из сохранения)
    //    → currentLevel = 5 (из сохранения)
}
```

## 📊 Сравнение

| Параметр | PlayButton (Новая игра) | SavesButton (Продолжить) |
|----------|-------------------------|--------------------------|
| **Действие** | Создает новое сохранение | Загружает последнее |
| **Очки** | 0 | Из сохранения |
| **Уровень** | 1 | Из сохранения |
| **Прогресс** | Обнулен | Сохранен |
| **Слот** | Первый свободный | Последний по дате |

## 🎯 Примеры Использования

### Сценарий 1: Первый запуск

```
Игрок запускает игру впервые

PlayButton ("Новая игра"):
→ Создает сохранение в слоте 0
→ Очки: 0, Уровень: 1
→ Игра начинается

SavesButton ("Продолжить"):
→ Нет сохранений
→ Создает новое сохранение
→ Очки: 0, Уровень: 1
→ Игра начинается

Результат одинаковый!
```

### Сценарий 2: Есть сохранение с прогрессом

```
Слот 0: Очки 1250, Уровень 5, Дата: 15.12.2024 18:30

PlayButton ("Новая игра"):
→ Создает НОВОЕ сохранение в слоте 1
→ Очки: 0, Уровень: 1
→ Игра начинается С НАЧАЛА
→ Старое сохранение (слот 0) остается

SavesButton ("Продолжить"):
→ Загружает слот 0 (последнее)
→ Очки: 1250, Уровень: 5
→ Игра ПРОДОЛЖАЕТСЯ
```

### Сценарий 3: Все слоты заняты

```
Слот 0: Очки 500, Уровень 2
Слот 1: Очки 1000, Уровень 4
Слот 2: Очки 1500, Уровень 6 ← Последнее

PlayButton ("Новая игра"):
→ Все слоты заняты!
→ ПЕРЕЗАПИСЫВАЕТ слот 0
→ Очки: 0, Уровень: 1
→ Игра начинается С НАЧАЛА
→ Старое сохранение в слоте 0 УДАЛЕНО

SavesButton ("Продолжить"):
→ Загружает слот 2 (последнее)
→ Очки: 1500, Уровень: 6
→ Игра ПРОДОЛЖАЕТСЯ
```

## 🔍 Что Происходит в InGameSaveManager

### При загрузке сцены:

```csharp
void Awake()
{
    // Получаем индекс слота из PlayerPrefs
    currentSlotIndex = PlayerPrefs.GetInt("CurrentSaveSlot", 0);
    
    // Загружаем сохранение из этого слота
    currentSaveData = SaveSystem.LoadFromSlot(currentSlotIndex);
    
    // Применяем данные к игре
    ApplySaveData();
}

void ApplySaveData()
{
    // Восстанавливаем время игры
    playTime = currentSaveData.playTime;
    
    // Применяем счет
    ScoreManager.Instance.SetScore(currentSaveData.currentScore);
    ScoreManager.Instance.SetHighScore(currentSaveData.highScore);
}
```

### Разница:

**PlayButton создает новое сохранение:**
```json
{
  "currentScore": 0,
  "highScore": 0,
  "currentLevel": 1,
  "playTime": 0,
  "saveDateTime": "15.12.2024 19:00"
}
```

**SavesButton загружает существующее:**
```json
{
  "currentScore": 1250,
  "highScore": 2000,
  "currentLevel": 5,
  "playTime": 3600,
  "saveDateTime": "15.12.2024 18:30"
}
```

## 🎨 Рекомендации по UI

### PlayButton (Новая игра):
- **Текст:** "Новая игра" или "New Game"
- **Цвет:** Синий (#2196F3)
- **Иконка:** ➕ или 🎮
- **Позиция:** Первая кнопка

### SavesButton (Продолжить):
- **Текст:** "Продолжить" или "Continue"
- **Цвет:** Зеленый (#4CAF50)
- **Иконка:** ▶️ или 💾
- **Позиция:** Вторая кнопка

### Расположение:
```
MenuPanel
├── PlayButton    [Новая игра]    (синяя)
├── SavesButton   [Продолжить]    (зеленая)
├── SettingsButton [Настройки]
└── ExitButton     [Выход]
```

## ⚠️ Важные Моменты

### 1. PlayButton всегда создает новое сохранение
- Если есть свободные слоты → создает в первом свободном
- Если все заняты → перезаписывает слот 0
- **Старый прогресс в слоте 0 будет потерян!**

### 2. SavesButton всегда загружает последнее
- Проверяет все 3 слота
- Выбирает самое свежее по дате
- Если нет сохранений → создает новое (как PlayButton)

### 3. Автосохранение
- Во время игры нажмите F5 для сохранения
- Автосохранение каждые 5 минут
- Сохраняется в текущий слот (из PlayerPrefs)

## 🐛 Решение Проблем

### PlayButton не обнуляет очки
**Проблема:** Игра продолжается со старыми очками
**Причина:** Не создается новое сохранение
**Решение:**
1. Проверьте, что OnPlayButton() вызывает CreateNewSave()
2. Убедитесь, что PlayerPrefs сохраняет новый индекс слота
3. Проверьте InGameSaveManager.LoadCurrentSave()

### SavesButton начинает новую игру
**Проблема:** Не загружает сохранение, начинает с нуля
**Причина:** Нет сохранений или неправильная дата
**Решение:**
1. Создайте сохранение (F5 в игре)
2. Проверьте формат даты: dd.MM.yyyy HH:mm
3. Убедитесь, что SaveSystem.GetLastSave() возвращает данные

### Все слоты заняты
**Проблема:** PlayButton не работает
**Причина:** Нет свободных слотов
**Решение:**
1. PlayButton автоматически перезапишет слот 0
2. Или удалите старые сохранения вручную
3. Или увеличьте MAX_SAVE_SLOTS в SaveSystem

## ✅ Чек-лист

- [ ] PlayButton привязана к MainMenu.OnPlayButton()
- [ ] SavesButton привязана к MainMenu.OnSavesButton()
- [ ] Текст кнопок правильный ("Новая игра" и "Продолжить")
- [ ] PlayButton создает новое сохранение (проверено)
- [ ] SavesButton загружает последнее (проверено)
- [ ] Очки обнуляются при новой игре
- [ ] Очки сохраняются при продолжении
- [ ] F5 сохраняет прогресс в игре

## 🎉 Итог

Теперь кнопки работают правильно:
- **PlayButton** - всегда новая игра с нуля (очки = 0)
- **SavesButton** - продолжить с последнего сохранения (очки сохранены)

Игроки могут выбирать! 🚀
