# Система заказов - Инструкция по настройке

## ✅ Созданные скрипты

1. **ScoreManager.cs** - управление очками
2. **OrderSystem.cs** - управление заказами
3. **OrderDisplay.cs** - отображение заказа на PC
4. **UnderCakePlate.cs** - подсчет коржей на тарелке

---

## 🔧 Настройка в Unity

### 1. Создать пустой GameObject для ScoreManager

```
1. Hierarchy → Create Empty
2. Назвать: "ScoreManager"
3. Add Component → ScoreManager
4. Создать TextMeshPro для очков:
   - Canvas → UI → Text - TextMeshPro
   - Назвать: "ScoreText"
   - Перетащить в поле Score Text компонента ScoreManager
```

### 2. Создать пустой GameObject для OrderSystem

```
1. Hierarchy → Create Empty
2. Назвать: "OrderSystem"
3. Add Component → OrderSystem
4. Настроить:
   - Min Korzh Count: 2 (минимум коржей)
   - Max Korzh Count: 5 (максимум коржей)
```

### 3. Настроить PC для отображения заказа

```
1. Найти объект PC в иерархии
2. Add Component → OrderDisplay
3. Создать TextMeshPro для заказа:
   - Canvas → UI → Text - TextMeshPro
   - Назвать: "OrderText"
   - Настроить размер и позицию над PC
   - Перетащить в поле Order Text компонента OrderDisplay
```

### 4. Настроить тарелку UnderCake

```
1. Найти объект UnderCake в иерархии
2. Add Component → UnderCakePlate
3. Настроить:
   - Check Radius: 2 (радиус проверки коржей)
   - Stack Tolerance: 0.3 (допуск для стопки)
   - Show Debug Info: true (для отладки)
```

### 5. Добавить Collider2D к тарелке (если нет)

```
1. UnderCake → Add Component → Circle Collider 2D
2. Настроить:
   - Is Trigger: true
   - Radius: подобрать по размеру тарелки
```

---

## 🎯 Как это работает

### Генерация заказа:

```
OrderSystem.Start()
  ↓
GenerateNewOrder()
  ↓
Random.Range(2, 5) → например 3 коржа
  ↓
OrderDisplay.UpdateOrderDisplay()
  ↓
На PC показывается: "ЗАКАЗ: 3 коржей"
```

### Проверка стопки:

```
UnderCakePlate.Update() (каждый кадр)
  ↓
CheckKorzhsOnPlate()
  ↓
Physics2D.OverlapCircleAll() → находит коржи
  ↓
Фильтрует: только коржи НЕ в руках
  ↓
CountKorzhsInStack() → считает стопку
  ↓
Сортирует по высоте (Y)
  ↓
Проверяет расстояние между коржами
  ↓
Если расстояние < 0.3 → в стопке
  ↓
Возвращает количество коржей в стопке
```

### Выполнение заказа:

```
Стопка на тарелке: 3 коржа
Заказ: 3 коржа
  ↓
OrderSystem.CheckOrder(3) → true
  ↓
UnderCakePlate.CompleteOrder()
  ↓
Destroy всех коржей на тарелке
  ↓
OrderSystem.CompleteOrder()
  ↓
ScoreManager.AddScore(1) → +1 очко
  ↓
GenerateNewOrder() → новый заказ
  ↓
OrderDisplay обновляется
```

---

## 📊 Алгоритм подсчета стопки

### Пример 1: Правильная стопка

```
Коржи на тарелке:
  Корж 3: Y = 2.5
  Корж 2: Y = 2.2  ← расстояние 0.3 ✅
  Корж 1: Y = 1.9  ← расстояние 0.3 ✅
  Тарелка: Y = 1.6

Результат: 3 коржа в стопке ✅
```

### Пример 2: Прерванная стопка

```
Коржи на тарелке:
  Корж 3: Y = 3.0
  Корж 2: Y = 2.2  ← расстояние 0.8 ❌ (> 0.3)
  Корж 1: Y = 1.9
  Тарелка: Y = 1.6

Результат: 1 корж в стопке (только Корж 3)
```

### Пример 3: Две отдельные стопки

```
Коржи на тарелке:
  Корж 4: Y = 3.5
  Корж 3: Y = 3.2  ← стопка 2
  Корж 2: Y = 2.0  ← большой разрыв
  Корж 1: Y = 1.7  ← стопка 1
  Тарелка: Y = 1.4

Результат: 2 коржа в стопке (Корж 3 и 4)
(считается только верхняя стопка)
```

---

## ⚙️ Настройки

### Stack Tolerance (допуск стопки):

```
0.3 - строгий (коржи должны быть очень близко)
0.5 - средний (рекомендуется)
1.0 - мягкий (коржи могут быть далеко)
```

### Check Radius (радиус проверки):

```
1.0 - маленькая тарелка
2.0 - средняя тарелка (рекомендуется)
3.0 - большая тарелка
```

### Min/Max Korzh Count:

```
Min: 2, Max: 5 - легкие заказы
Min: 3, Max: 7 - средние заказы
Min: 5, Max: 10 - сложные заказы
```

---

## 🎮 Игровой процесс

### Шаг 1: Получить заказ

```
Игрок смотрит на PC
  ↓
Видит: "ЗАКАЗ: 4 коржей"
  ↓
Нужно собрать стопку из 4 коржей
```

### Шаг 2: Собрать коржи

```
Игрок подбирает Корж 1
  ↓
Кладет на тарелку UnderCake
  ↓
Подбирает Корж 2
  ↓
Кладет НА Корж 1
  ↓
Подбирает Корж 3
  ↓
Кладет НА Корж 2
  ↓
Подбирает Корж 4
  ↓
Кладет НА Корж 3
  ↓
Стопка из 4 коржей готова! 🎂
```

### Шаг 3: Выполнить заказ

```
UnderCakePlate проверяет стопку
  ↓
Считает: 4 коржа в стопке
  ↓
Сравнивает с заказом: 4 == 4 ✅
  ↓
Заказ выполнен!
  ↓
Коржи исчезают
  ↓
+1 очко
  ↓
Новый заказ: "ЗАКАЗ: 3 коржей"
```

---

## 🐛 Отладка

### Включить Debug Info:

```
UnderCakePlate → Show Debug Info: true

В консоли будет:
"Коржей в стопке на тарелке: 3"
"Заказ выполнен! 3 коржей"
```

### Визуализация радиуса:

```
Выбрать UnderCake в иерархии
  ↓
В Scene View появится зеленая окружность
  ↓
Это радиус проверки коржей
  ↓
Коржи внутри окружности учитываются
```

### Проверка стопки:

```
1. Положить коржи на тарелку
2. Смотреть в консоль
3. Должно показывать количество коржей
4. Если не показывает:
   - Проверить что коржи имеют PickupObject
   - Проверить что objectType = Korzh
   - Проверить что коржи НЕ в руках
```

---

## ✅ Чек-лист настройки

- [ ] ScoreManager создан и настроен
- [ ] OrderSystem создан и настроен
- [ ] OrderDisplay добавлен к PC
- [ ] UI Text для заказа создан
- [ ] UnderCakePlate добавлен к тарелке
- [ ] Collider2D добавлен к тарелке
- [ ] UI Text для очков создан
- [ ] Все ссылки в Inspector заполнены
- [ ] Протестировано в игре

---

## 📝 Важные замечания

### О подсчете стопки:
- Считаются **только коржи друг на друге**
- Коржи **НЕ в руках**
- Расстояние между коржами **< Stack Tolerance**
- Сортировка **по высоте (Y)**

### О заказах:
- Генерируются **случайно**
- От **Min** до **Max** коржей
- Новый заказ **после выполнения**
- Отображается **на PC**

### Об очках:
- **+1 очко** за каждый заказ
- Отображаются **в UI**
- Сохраняются **в ScoreManager**

### О коржах:
- Должны иметь **PickupObject**
- **objectType = Korzh**
- **НЕ в руках** (isPickedUp = false)
- В радиусе **Check Radius**

---

**Система заказов готова! Игрок может собирать торты и получать очки!** 🎂🎉
