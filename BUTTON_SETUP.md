# Настройка кнопки проверки заказа

## ✅ Что изменено

### UnderCakePlate.cs:
- **Убрана** автоматическая проверка из `Update()`
- **Добавлен** метод `CheckOrderButton()` для кнопки
- Проверка теперь **только по нажатию кнопки**

### OrderSystem.cs:
- **Добавлен** метод `CompleteOrderCorrect()` - правильный заказ (+1 очко)
- **Добавлен** метод `CompleteOrderIncorrect()` - неправильный заказ (без очков)

---

## 🔧 Настройка кнопки в Unity

### 1. Создать кнопку

```
1. Canvas → UI → Button - TextMeshPro
2. Назвать: "CheckOrderButton"
3. Настроить текст кнопки: "Проверить заказ"
4. Настроить позицию (например, внизу экрана)
```

### 2. Привязать метод к кнопке

```
1. Выбрать кнопку "CheckOrderButton"
2. В Inspector найти компонент Button
3. В разделе "On Click ()" нажать "+"
4. Перетащить объект UnderCake в поле "None (Object)"
5. В выпадающем меню выбрать:
   UnderCakePlate → CheckOrderButton()
```

### 3. Проверить настройку

```
1. Запустить игру
2. Положить коржи на тарелку
3. Нажать кнопку "Проверить заказ"
4. Смотреть в консоль:
   - ✅ "ЗАКАЗ ПРАВИЛЬНЫЙ!" - если совпадает
   - ❌ "ЗАКАЗ НЕПРАВИЛЬНЫЙ!" - если не совпадает
```

---

## 🎯 Как это работает

### Правильный заказ:

```
Заказ: 3 коржа
Игрок собрал: 3 коржа
  ↓
Нажал кнопку "Проверить заказ"
  ↓
UnderCakePlate.CheckOrderButton()
  ↓
Подсчет: 3 коржа в стопке
  ↓
Проверка: 3 == 3 ✅
  ↓
OrderSystem.CompleteOrderCorrect()
  ↓
+1 очко ✅
  ↓
Коржи исчезают 🗑️
  ↓
Новый заказ: "ЗАКАЗ: 4 коржей"
```

### Неправильный заказ:

```
Заказ: 3 коржа
Игрок собрал: 2 коржа
  ↓
Нажал кнопку "Проверить заказ"
  ↓
UnderCakePlate.CheckOrderButton()
  ↓
Подсчет: 2 коржа в стопке
  ↓
Проверка: 2 != 3 ❌
  ↓
OrderSystem.CompleteOrderIncorrect(2)
  ↓
Очков НЕТ ❌
  ↓
Коржи исчезают 🗑️
  ↓
Новый заказ: "ЗАКАЗ: 5 коржей"
```

---

## 📊 Логика проверки

### CheckOrderButton():

```csharp
public void CheckOrderButton()
{
    // 1. Найти все коржи на тарелке
    OverlapCircleAll() → находит коржи
    
    // 2. Подсчитать стопку
    CountKorzhsInStack() → считает
    
    // 3. Проверить заказ
    bool isCorrect = OrderSystem.CheckOrder(stackCount);
    
    // 4. Если правильно:
    if (isCorrect)
    {
        RemoveAllKorzhs(); // Удалить торт
        CompleteOrderCorrect(); // +1 очко
    }
    // 5. Если неправильно:
    else
    {
        RemoveAllKorzhs(); // Все равно удалить торт
        CompleteOrderIncorrect(); // Без очков
    }
}
```

---

## 🎮 Игровой процесс

### Шаг 1: Получить заказ

```
На PC: "ЗАКАЗ: 3 коржей"
```

### Шаг 2: Собрать коржи

```
Игрок подбирает коржи
  ↓
Кладет на тарелку UnderCake
  ↓
Складывает друг на друга
  ↓
Стопка из 3 коржей готова
```

### Шаг 3: Нажать кнопку

```
Игрок нажимает "Проверить заказ"
  ↓
Система проверяет стопку
  ↓
Если правильно: +1 очко ✅
Если неправильно: без очков ❌
  ↓
Торт исчезает в любом случае 🗑️
  ↓
Новый заказ
```

---

## 🐛 Отладка

### Логи в консоли:

**При нажатии кнопки:**
```
"Проверка заказа: коржей в стопке = 3"
```

**Если правильно:**
```
"✅ ЗАКАЗ ПРАВИЛЬНЫЙ! 3 коржей"
"✅ Заказ выполнен ПРАВИЛЬНО! 3 коржей"
"🗑️ Все коржи удалены с тарелки"
"Очки добавлены! Текущий счет: 1"
"Новый заказ: 4 коржей"
```

**Если неправильно:**
```
"❌ ЗАКАЗ НЕПРАВИЛЬНЫЙ! Нужно: 3, Дано: 2"
"❌ Заказ выполнен НЕПРАВИЛЬНО! Нужно: 3, Дано: 2"
"🗑️ Все коржи удалены с тарелки"
"Новый заказ: 5 коржей"
```

---

## ⚙️ Настройки кнопки

### Внешний вид:

```
Button:
  - Text: "Проверить заказ" / "Check Order"
  - Font Size: 24-32
  - Color: зеленый для правильного действия
  - Width: 200-300
  - Height: 60-80
```

### Позиция:

```
Варианты:
1. Внизу по центру экрана
2. Рядом с тарелкой UnderCake
3. Рядом с PC (где заказ)
```

### Стиль:

```
Normal Color: зеленый
Highlighted Color: светло-зеленый
Pressed Color: темно-зеленый
Disabled Color: серый
```

---

## ✅ Чек-лист

- [ ] Кнопка создана в Canvas
- [ ] Текст кнопки настроен
- [ ] Метод CheckOrderButton() привязан к On Click()
- [ ] UnderCake перетащен в поле объекта
- [ ] Протестировано с правильным заказом
- [ ] Протестировано с неправильным заказом
- [ ] Коржи исчезают в обоих случаях
- [ ] Очки добавляются только при правильном заказе

---

## 📝 Важные замечания

### О кнопке:
- **Нажимается вручную** игроком
- **Проверяет заказ** один раз при нажатии
- **Удаляет торт** в любом случае

### О проверке:
- **Правильно** → +1 очко + новый заказ
- **Неправильно** → без очков + новый заказ
- **Торт исчезает** всегда

### О стопке:
- Считаются **только коржи друг на друге**
- Расстояние между коржами **< 0.3**
- Коржи **НЕ в руках**

---

## 🎨 Пример UI

```
┌─────────────────────────────┐
│         ЗАКАЗ: 3 коржей     │  ← OrderDisplay на PC
└─────────────────────────────┘

         🎂 🎂 🎂              ← Коржи на тарелке
        ─────────              ← UnderCake
        
┌─────────────────────────────┐
│    [Проверить заказ]        │  ← Кнопка
└─────────────────────────────┘

┌─────────────────────────────┐
│         Очки: 5             │  ← ScoreManager
└─────────────────────────────┘
```

---

**Кнопка настроена! Игрок может проверять заказы вручную!** 🎉
