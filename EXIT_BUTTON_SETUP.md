# 🚪 Кнопка "Выйти" в Игре - Инструкция

## 📋 Описание

Добавлена кнопка "Выйти" в игре, которая:
- ✅ Сохраняет текущий прогресс
- ✅ Возвращает в главное меню
- ✅ Работает через меню паузы (ESC)

## 🎯 Что Создано

### Новый Скрипт:
- **`PauseMenuManager.cs`** - управление меню паузы и кнопкой выхода

## 🔧 Настройка в Unity

### Шаг 1: Создайте UI Меню Паузы

В игровой сцене (GameScene):

#### 1.1 Создайте Canvas (если нет):
```
Hierarchy → Правый клик → UI → Canvas
```

#### 1.2 Создайте Панель Паузы:
```
Canvas
└── PauseMenuPanel (Panel)
    ├── Background (Image) - Полупрозрачный темный фон
    ├── Title (TextMeshPro) - "ПАУЗА"
    ├── ResumeButton (Button) - "Продолжить"
    └── ExitButton (Button) - "Выйти в меню"
```

### Шаг 2: Настройте PauseMenuPanel

1. Выберите **PauseMenuPanel**
2. Настройте размер:
   - Anchor: Center-Middle
   - Width: 400-500 px
   - Height: 300-400 px
3. Добавьте **Image** компонент:
   - Color: rgba(0, 0, 0, 0.8) - темный полупрозрачный

### Шаг 3: Создайте Кнопки

#### ResumeButton (Продолжить):
- **Размер:** 300x60 px
- **Текст:** "Продолжить"
- **Цвет:** Зеленый (#4CAF50)
- **Позиция:** Y: 50

#### ExitButton (Выйти):
- **Размер:** 300x60 px
- **Текст:** "Выйти в меню"
- **Цвет:** Красный (#F44336)
- **Позиция:** Y: -50

### Шаг 4: Добавьте PauseMenuManager

1. Создайте **пустой GameObject** в сцене:
   - Правый клик в Hierarchy → Create Empty
   - Назовите "PauseMenuManager"

2. Добавьте компонент **PauseMenuManager**:
   - Add Component → PauseMenuManager

3. Настройте поля в Inspector:

#### UI Элементы:
- **Pause Menu Panel** → перетащите PauseMenuPanel
- **Exit Button** → перетащите ExitButton
- **Resume Button** → перетащите ResumeButton

#### Настройки:
- **Pause Key:** Escape (по умолчанию)
- **Menu Scene Name:** "MainMenu" (или название вашей сцены меню)

### Шаг 5: Настройте Кнопки

#### ResumeButton:
1. Выберите кнопку
2. В компоненте **Button** → **OnClick()**:
   - Нажмите **+**
   - Перетащите GameObject **PauseMenuManager**
   - Выберите функцию: **PauseMenuManager.OnResumeButton()**

#### ExitButton:
1. Выберите кнопку
2. В компоненте **Button** → **OnClick()**:
   - Нажмите **+**
   - Перетащите GameObject **PauseMenuManager**
   - Выберите функцию: **PauseMenuManager.OnExitButton()**

### Шаг 6: Скройте Панель По Умолчанию

1. Выберите **PauseMenuPanel**
2. В Inspector **снимите галочку** в верхнем левом углу (деактивируйте)
3. Панель должна быть невидимой при запуске игры

## 🎮 Как Использовать

### В Игре:

#### Открыть Меню Паузы:
- Нажмите **ESC** (или настроенную клавишу)
- Игра остановится (Time.timeScale = 0)
- Появится меню паузы

#### Продолжить Игру:
- Нажмите кнопку **"Продолжить"**
- Или нажмите **ESC** снова
- Игра продолжится

#### Выйти в Меню:
- Нажмите кнопку **"Выйти в меню"**
- Игра автоматически сохранится
- Вернетесь в главное меню

## 📊 Логика Работы

### При Нажатии "Выйти":

```
1. PauseMenuManager.OnExitButton()
   ↓
2. InGameSaveManager.QuickSave()
   → Сохраняет текущий прогресс
   → Обновляет дату/время сохранения
   ↓
3. Time.timeScale = 1f
   → Возобновляет время (важно!)
   ↓
4. SceneManager.LoadScene("MainMenu")
   → Загружает главное меню
```

### Что Сохраняется:
- ✅ Текущие очки
- ✅ Лучший счет
- ✅ Уровень
- ✅ Время игры
- ✅ Статистика заказов

## 🎨 Рекомендуемый Дизайн

### Панель Паузы:
```
┌─────────────────────────────────┐
│                                 │
│           П А У З А             │
│                                 │
│   ┌─────────────────────────┐   │
│   │     Продолжить          │   │
│   └─────────────────────────┘   │
│                                 │
│   ┌─────────────────────────┐   │
│   │   Выйти в меню          │   │
│   └─────────────────────────┘   │
│                                 │
└─────────────────────────────────┘
```

### Цвета:
- **Фон панели:** rgba(0, 0, 0, 0.85) - темный
- **Заголовок:** #FFFFFF (белый), размер 36
- **Кнопка "Продолжить":** #4CAF50 (зеленый)
- **Кнопка "Выйти":** #F44336 (красный)

### Размеры:
- **Панель:** 400x300 px
- **Кнопки:** 300x60 px
- **Отступы:** 20 px между кнопками

## ⚙️ Дополнительные Возможности

### Добавить Кнопку "Настройки":

```csharp
[Header("Дополнительные Кнопки")]
public Button settingsButton; // Кнопка настроек

void Start()
{
    // ...
    if (settingsButton != null)
        settingsButton.onClick.AddListener(OnSettingsButton);
}

public void OnSettingsButton()
{
    // Открыть панель настроек
    Debug.Log("Открываем настройки");
}
```

### Добавить Подтверждение Выхода:

```csharp
[Header("Подтверждение")]
public GameObject confirmationPanel; // Панель подтверждения
public Button confirmYesButton;
public Button confirmNoButton;

public void OnExitButton()
{
    // Показываем панель подтверждения
    if (confirmationPanel != null)
    {
        confirmationPanel.SetActive(true);
    }
    else
    {
        ExitToMenu(); // Выход без подтверждения
    }
}

void ExitToMenu()
{
    // Сохраняем и выходим
    if (InGameSaveManager.Instance != null)
        InGameSaveManager.Instance.QuickSave();
    
    Time.timeScale = 1f;
    SceneManager.LoadScene(menuSceneName);
}
```

### Добавить Звуки:

```csharp
[Header("Звуки")]
public AudioClip pauseSound;
public AudioClip resumeSound;
public AudioClip exitSound;
private AudioSource audioSource;

void Awake()
{
    audioSource = gameObject.AddComponent<AudioSource>();
}

public void PauseGame()
{
    // ...
    PlaySound(pauseSound);
}

void PlaySound(AudioClip clip)
{
    if (clip != null && audioSource != null)
        audioSource.PlayOneShot(clip);
}
```

## 🐛 Решение Проблем

### Игра не останавливается при паузе
**Проблема:** Объекты продолжают двигаться
**Решение:**
- Проверьте, что `Time.timeScale = 0f` вызывается
- Убедитесь, что движение объектов использует `Time.deltaTime`

### Кнопки не работают в паузе
**Проблема:** Кнопки не кликаются
**Решение:**
- Canvas должен быть в режиме **Screen Space - Overlay**
- Или используйте **Render Mode: Screen Space - Camera**

### Игра не сохраняется
**Проблема:** При выходе прогресс теряется
**Решение:**
- Проверьте, что `InGameSaveManager.Instance != null`
- Проверьте консоль на ошибки
- Убедитесь, что QuickSave() вызывается

### Time.timeScale остается 0
**Проблема:** После выхода в меню все замерло
**Решение:**
- Убедитесь, что `Time.timeScale = 1f` вызывается перед LoadScene
- Добавьте в MainMenu.Start():
```csharp
void Start()
{
    Time.timeScale = 1f; // Гарантируем нормальное время
    ShowMainMenu();
}
```

## ✅ Чек-лист Настройки

- [ ] Создан PauseMenuPanel в GameScene
- [ ] Добавлены кнопки ResumeButton и ExitButton
- [ ] Создан GameObject с PauseMenuManager
- [ ] Заполнены все ссылки в Inspector
- [ ] OnClick() кнопок настроены
- [ ] PauseMenuPanel деактивирована по умолчанию
- [ ] Проверено: ESC открывает меню
- [ ] Проверено: "Продолжить" работает
- [ ] Проверено: "Выйти" сохраняет и возвращает в меню
- [ ] Настроен дизайн панели

## 🎯 Пример Использования

### Сценарий 1: Пауза в игре
```
Игрок играет
→ Нажимает ESC
→ Игра останавливается
→ Появляется меню паузы
→ Нажимает "Продолжить"
→ Игра продолжается
```

### Сценарий 2: Выход в меню
```
Игрок играет (очки: 500)
→ Нажимает ESC
→ Нажимает "Выйти в меню"
→ Игра сохраняется (очки: 500)
→ Загружается главное меню
→ Нажимает "Продолжить"
→ Игра загружается с очками 500 ✅
```

### Сценарий 3: Быстрый выход
```
Игрок играет
→ Нажимает ESC
→ Нажимает "Выйти в меню"
→ Возвращается в меню
→ Прогресс сохранен!
```

## 🎉 Готово!

Теперь у вас есть полнофункциональное меню паузы с кнопкой выхода:
- ✅ ESC открывает/закрывает меню
- ✅ Игра останавливается на паузе
- ✅ Кнопка "Выйти" сохраняет прогресс
- ✅ Автоматический возврат в главное меню

Игроки могут безопасно выходить из игры! 🚀
