# Настройка системы случайных клиентов

## 📋 Обзор
Система автоматически показывает случайного клиента (2D спрайт) при каждом новом заказе.

---

## 🎨 Шаг 1: Подготовка спрайтов клиентов

### Импорт спрайтов:
1. Создайте папку: `Assets/Sprites/Customers/`
2. Поместите туда спрайты клиентов (PNG файлы)
3. Рекомендуемый размер: **256x256** или **512x512** пикселей

### Настройка спрайтов в Unity:
```
Выберите спрайт → Inspector:
- Texture Type: Sprite (2D and UI)
- Pixels Per Unit: 100
- Filter Mode: Bilinear
- Compression: Normal Quality
→ Apply
```

---

## 🖼️ Шаг 2: Создание UI для клиента

### В Canvas создайте Image для клиента:

```
1. Canvas → ПКМ → UI → Image
2. Назвать: "CustomerImage"
```

### Настройка CustomerImage:

```
Rect Transform:
  - Anchor Presets: Top Left (или где хотите)
  - Pos X: 100
  - Pos Y: -100
  - Width: 200
  - Height: 200

Image:
  - Source Image: (оставить пустым - будет меняться автоматически)
  - Color: белый (255, 255, 255, 255)
  - Raycast Target: ✓ (можно отключить)
  - Preserve Aspect: ✓ (сохранять пропорции)
```

---

## 🔧 Шаг 3: Настройка OrderDisplay

### Найдите объект с OrderDisplay:
```
Hierarchy → найдите объект с компонентом OrderDisplay
(обычно это Canvas или OrderPanel)
```

### Настройте параметры:

```
OrderDisplay (Script):
  UI:
    - Order Text: [перетащить OrderText]
    - Order Panel: [опционально]
  
  Клиенты:
    - Customer Image: [перетащить CustomerImage]
    - Customer Sprites:
        Size: [количество клиентов, например 5]
        Element 0: [спрайт клиента 1]
        Element 1: [спрайт клиента 2]
        Element 2: [спрайт клиента 3]
        Element 3: [спрайт клиента 4]
        Element 4: [спрайт клиента 5]
```

---

## 🎯 Как это работает:

1. **При запуске игры** - появляется случайный клиент
2. **При новом заказе** - клиент меняется на другого случайного
3. **Автоматически** - не требует дополнительного кода

---

## 🎨 Дополнительные настройки

### Добавить рамку вокруг клиента:

```
CustomerImage → Add Component → Outline
  - Effect Color: черный
  - Effect Distance: (2, -2)
```

### Добавить тень:

```
CustomerImage → Add Component → Shadow
  - Effect Color: черный (с прозрачностью)
  - Effect Distance: (2, -2)
```

### Сделать фон для клиента:

```
1. Canvas → ПКМ → UI → Image
2. Назвать: "CustomerBackground"
3. Поместить ПОД CustomerImage в Hierarchy
4. Настроить размер чуть больше CustomerImage
5. Цвет: светло-серый или любой другой
```

---

## 🐛 Проверка работы:

1. **Запустите игру**
2. **Проверьте** - появился ли клиент рядом с заказом
3. **Выполните заказ** - клиент должен смениться на другого
4. **Если клиент не появляется:**
   - Проверьте что CustomerImage назначен в OrderDisplay
   - Проверьте что массив Customer Sprites заполнен
   - Проверьте что CustomerImage включен (enabled)

---

## 💡 Советы:

- **Минимум 3-5 разных клиентов** для разнообразия
- **Можно использовать разные стили:** животные, люди, роботы и т.д.
- **Размер спрайтов:** лучше делать квадратными для удобства
- **Формат:** PNG с прозрачным фоном

---

## 📝 Пример структуры:

```
Canvas
├── OrderText (TextMeshPro)
├── CustomerBackground (Image) - опционально
└── CustomerImage (Image) ← сюда назначаем спрайты
```

---

## ✅ Готово!

Теперь при каждом новом заказе будет появляться случайный клиент из вашего списка спрайтов.
