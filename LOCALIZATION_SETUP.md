# 🌍 Система Локализации - Инструкция

## 📋 Описание

Создана полная система локализации с поддержкой:
- ✅ Русский язык (ru)
- ✅ Английский язык (en)
- ✅ Автоматическое обновление всех текстов
- ✅ Сохранение выбранного языка
- ✅ Кнопки выбора языка в настройках

## 🎯 Что Создано

### Новые Скрипты:

1. **`LocalizationSystem.cs`** - основная система локализации
   - Хранит все переводы
   - Управляет текущим языком
   - Сохраняет/загружает язык

2. **`LocalizedText.cs`** - компонент для UI текста
   - Автоматически переводит текст
   - Обновляется при смене языка

3. **`LanguageSelector.cs`** - выбор языка в настройках
   - Кнопки "Русский" и "English"
   - Визуальная индикация выбранного языка

## 🔧 Настройка в Unity

### Шаг 1: Создайте LocalizationSystem

1. В **любой сцене** (лучше в главном меню):
   - Правый клик в Hierarchy → Create Empty
   - Назовите "LocalizationSystem"

2. Добавьте компонент **LocalizationSystem**:
   - Add Component → LocalizationSystem

3. Настройте:
   - **Current Language:** "ru" (по умолчанию русский)

**Важно:** LocalizationSystem использует DontDestroyOnLoad, поэтому будет работать во всех сценах!

### Шаг 2: Добавьте Кнопки Языков в Настройки

В панели настроек (SettingsPanel):

#### 2.1 Создайте Секцию Языка:

```
SettingsPanel
└── LanguageSection (Empty GameObject)
    ├── LanguageTitle (TextMeshPro) - "Язык" / "Language"
    ├── RussianButton (Button) - "Русский"
    └── EnglishButton (Button) - "English"
```

#### 2.2 Настройте Кнопки:

**RussianButton:**
- Размер: 150x50 px
- Текст: "Русский"
- Цвет: Белый (по умолчанию)

**EnglishButton:**
- Размер: 150x50 px
- Текст: "English"
- Цвет: Белый (по умолчанию)

#### 2.3 Добавьте LanguageSelector:

1. Выберите **LanguageSection** (или SettingsPanel)
2. Add Component → **LanguageSelector**
3. Настройте поля:
   - **Russian Button** → перетащите RussianButton
   - **English Button** → перетащите EnglishButton
   - **Selected Color** → Зеленый (0.3, 0.8, 0.3, 1)
   - **Normal Color** → Белый (1, 1, 1, 1)

#### 2.4 Настройте OnClick():

**RussianButton:**
- OnClick() → LanguageSelector.OnRussianButton()

**EnglishButton:**
- OnClick() → LanguageSelector.OnEnglishButton()

### Шаг 3: Добавьте Локализацию к UI Текстам

Для каждого текста, который нужно перевести:

#### 3.1 Выберите TextMeshPro элемент

Например, кнопка "Играть" в главном меню.

#### 3.2 Добавьте LocalizedText:

- Add Component → **LocalizedText**

#### 3.3 Настройте Ключ:

- **Localization Key:** "menu_play"

#### 3.4 Текст Обновится Автоматически!

При запуске игры текст изменится на:
- Русский: "Играть"
- English: "Play"

### Шаг 4: Примеры Локализации UI

#### Главное Меню:

| Элемент | Localization Key | RU | EN |
|---------|------------------|----|----|
| Кнопка "Играть" | menu_play | Играть | Play |
| Кнопка "Продолжить" | menu_continue | Продолжить | Continue |
| Кнопка "Настройки" | menu_settings | Настройки | Settings |
| Кнопка "Выход" | menu_exit | Выход | Exit |

#### Настройки:

| Элемент | Localization Key | RU | EN |
|---------|------------------|----|----|
| Заголовок | settings_title | Настройки | Settings |
| Секция языка | settings_language | Язык | Language |
| Кнопка "Назад" | menu_back | Назад | Back |

#### Игра:

| Элемент | Localization Key | RU | EN |
|---------|------------------|----|----|
| Очки | game_score | Очки: {0} | Score: {0} |
| Рекорд | game_highscore | Рекорд: {0} | High Score: {0} |
| Уровень | game_level | Уровень {0} | Level {0} |

## 🎮 Использование в Коде

### Получить Перевод:

```csharp
// Простой перевод
string text = LocalizationSystem.Instance.GetText("menu_play");
// Результат: "Играть" (ru) или "Play" (en)

// С форматированием
string scoreText = LocalizationSystem.Instance.GetText("game_score", 1250);
// Результат: "Очки: 1250" (ru) или "Score: 1250" (en)
```

### Изменить Язык:

```csharp
// Установить русский
LocalizationSystem.Instance.SetLanguage("ru");

// Установить английский
LocalizationSystem.Instance.SetLanguage("en");

// Получить текущий язык
string currentLang = LocalizationSystem.Instance.GetCurrentLanguage();
```

### Обновить Динамический Текст:

```csharp
public class ScoreDisplay : MonoBehaviour
{
    public LocalizedText scoreText;
    
    void UpdateScore(int score)
    {
        // Обновляем текст с форматированием
        scoreText.UpdateText(score);
    }
}
```

## 📝 Добавление Новых Переводов

### В LocalizationSystem.cs:

Найдите метод `InitializeTranslations()` и добавьте:

```csharp
// Новый перевод
AddTranslation("my_key", "ru", "Мой текст");
AddTranslation("my_key", "en", "My text");
```

### Примеры:

```csharp
// Кнопки
AddTranslation("button_start", "ru", "Начать");
AddTranslation("button_start", "en", "Start");

// Сообщения
AddTranslation("msg_victory", "ru", "Победа!");
AddTranslation("msg_victory", "en", "Victory!");

// С форматированием
AddTranslation("player_health", "ru", "Здоровье: {0}");
AddTranslation("player_health", "en", "Health: {0}");
```

## 🎨 Визуальная Индикация Языка

### Выбранная Кнопка:
- Цвет: Зеленый (#4DCC4D)
- Показывает текущий язык

### Невыбранная Кнопка:
- Цвет: Белый (#FFFFFF)

### Пример:
```
[Русский]  ← Зеленая (выбрана)
[English]  ← Белая
```

## 🔄 Автоматическое Обновление

При смене языка:
1. LocalizationSystem.SetLanguage() вызывается
2. Язык сохраняется в PlayerPrefs
3. Все LocalizedText компоненты обновляются автоматически
4. UI мгновенно переключается на новый язык

## 📊 Список Всех Ключей

### Главное Меню:
- `menu_play` - Играть / Play
- `menu_continue` - Продолжить / Continue
- `menu_settings` - Настройки / Settings
- `menu_exit` - Выход / Exit
- `menu_back` - Назад / Back

### Настройки:
- `settings_title` - Настройки / Settings
- `settings_language` - Язык / Language
- `settings_russian` - Русский / Russian
- `settings_english` - Английский / English

### Игра:
- `game_score` - Очки: {0} / Score: {0}
- `game_highscore` - Рекорд: {0} / High Score: {0}
- `game_level` - Уровень {0} / Level {0}
- `game_pause` - Пауза / Pause
- `game_resume` - Продолжить / Resume
- `game_exit_to_menu` - Выйти в меню / Exit to Menu

### Сохранения:
- `save_saved` - Игра сохранена / Game Saved
- `save_autosave` - Автосохранение / Autosave
- `save_quicksave` - Быстрое сохранение / Quick Save
- `save_new` - Новое сохранение / New Save

### Заказы:
- `order_correct` - Правильно! / Correct!
- `order_wrong` - Неправильно! / Wrong!
- `order_completed` - Заказ выполнен / Order Completed

### Враги:
- `enemy_defeated` - Враг побежден / Enemy Defeated
- `enemy_escaped` - Враг сбежал / Enemy Escaped

## 🛠️ Интеграция с Существующими Скриптами

### ScoreManager:

```csharp
void UpdateScoreUI()
{
    if (scoreText != null)
    {
        // Используем локализацию
        string localizedText = LocalizationSystem.Instance.GetText("game_score", currentScore);
        scoreText.text = localizedText;
    }
}
```

### InGameSaveManager:

```csharp
void ShowSaveNotification(string messageKey)
{
    if (saveNotificationText != null)
    {
        string localizedMessage = LocalizationSystem.Instance.GetText(messageKey);
        saveNotificationText.text = localizedMessage;
        // ...
    }
}

public void QuickSave()
{
    // ...
    ShowSaveNotification("save_quicksave");
}
```

## 🎯 Быстрая Настройка

### Минимальная Конфигурация:

1. **Создайте LocalizationSystem** в главном меню
2. **Добавьте LanguageSelector** в настройки
3. **Добавьте LocalizedText** к основным кнопкам:
   - Играть → "menu_play"
   - Настройки → "menu_settings"
   - Выход → "menu_exit"

### Проверка:

1. Запустите игру
2. Откройте настройки
3. Нажмите "English"
4. Все тексты должны измениться на английский!

## 🐛 Решение Проблем

### Тексты не переводятся

**Проблема:** Текст остается на русском
**Решение:**
1. Проверьте, что LocalizationSystem создана в сцене
2. Проверьте, что у текста есть компонент LocalizedText
3. Проверьте правильность ключа локализации

### Кнопки языка не работают

**Проблема:** Клик не меняет язык
**Решение:**
1. Проверьте OnClick() кнопок
2. Убедитесь, что LanguageSelector привязан
3. Проверьте консоль на ошибки

### Язык не сохраняется

**Проблема:** После перезапуска язык сбрасывается
**Решение:**
1. Проверьте, что LocalizationSystem вызывает SaveLanguage()
2. PlayerPrefs должен сохраняться автоматически

### Новые переводы не работают

**Проблема:** Добавленный перевод не отображается
**Решение:**
1. Проверьте, что AddTranslation() вызывается в InitializeTranslations()
2. Перезапустите игру
3. Проверьте правильность ключа

## ✅ Чек-лист Настройки

- [ ] LocalizationSystem создана в сцене
- [ ] LanguageSelector добавлен в настройки
- [ ] Кнопки "Русский" и "English" созданы
- [ ] OnClick() кнопок настроены
- [ ] LocalizedText добавлен к основным UI элементам
- [ ] Ключи локализации заполнены
- [ ] Проверена смена языка в игре
- [ ] Язык сохраняется после перезапуска
- [ ] Все тексты переводятся правильно

## 🎉 Готово!

Теперь у вас полнофункциональная система локализации:
- ✅ Поддержка русского и английского
- ✅ Простое добавление новых переводов
- ✅ Автоматическое обновление UI
- ✅ Сохранение выбранного языка
- ✅ Удобные кнопки в настройках

Ваша игра теперь мультиязычная! 🌍
